spring:
  application:
    name: ${rootProjectName}-server

  profiles:
    active:
      - service
      - local

  datasource:
    url: "jdbc:h2:file:./db/graffiti"

  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true

  sql:
    init:
      mode: always
